

const initState = {
    common:{},
    user:{},
    classroom:{},
    apply:{}
};


const reducer = (state = initState, action) => {
    switch (action.type){
        default:
            return state;
    }
};


export default reducer;